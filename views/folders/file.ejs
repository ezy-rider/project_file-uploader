<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Drive - <%= file.name %></title>
  </head>
  <body>
    <h1><%= file.name %></h1>
    <a href="/folders/<%= folderId %>/<%= file.id %>/download">Download</a>

    <% for(let key in file) { %>
    <p><span><%= key %></span>: <%= file[key] %></p>
    <% } %>
  </body>
</html>
